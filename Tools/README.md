# Tools

Projeto de biblioteca (`netstandard2.0`) atualmente vazio, contendo apenas uma classe placeholder (`Class1`) sem nenhum membro implementado. Não possui `ProjectReference` para outros projetos da solução nem lógica funcional até o momento.

Parece ser um esqueleto criado para uma futura biblioteca de utilitários compartilhados, ainda não desenvolvida.

## Como compilar

```bash
dotnet build Tools/Tools.csproj
```

## Observação

Não há, hoje, nenhuma funcionalidade a documentar. Este README deve ser atualizado assim que o projeto ganhar código real.
